Exercise in API validation using JSON Schema

*Note: to actually utilize this I would put config.js in .gitignore.  I include it here for convenience of testing purposes.  Change that if used for production.